# axolotl-training-job.yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: gemma3-melanoma-training
spec:
  backoffLimit: 0
  template:
    metadata:
      annotations:
        gke-gcsfuse/volumes: "true"
    spec:
      nodeSelector:
        cloud.google.com/gke-accelerator: nvidia-tesla-a100
      restartPolicy: Never
      containers:
      - name: axolotl
        image: axolotlai/axolotl:main-latest
        command: ["/bin/bash", "-c"]
        args:
          - |
            # Create directory structure and symbolic link
            mkdir -p /mnt/gcs/your-project-melanoma-dataset
            ln -s /mnt/gcs/processed_images /mnt/gcs/your-project-melanoma-dataset/processed_images
            echo "Created symbolic link for image paths"
            
            # Now run the training
            cd /workspace/axolotl && python -m axolotl.cli.train /workspace/configs/gemma3-melanoma.yaml
        env:
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: /credentials/service-account.json
        - name: HUGGING_FACE_HUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: huggingface-credentials
              key: token
        - name: NCCL_DEBUG
          value: "INFO"
        resources:
          limits:
            nvidia.com/gpu: 1
          requests:
            memory: "32Gi"
            cpu: "8"
            ephemeral-storage: "10Gi"
            nvidia.com/gpu: 1
        volumeMounts:
        - name: config-volume
          mountPath: /workspace/configs
        - name: credentials
          mountPath: /credentials
        - name: model-storage
          mountPath: /outputs
        - name: gcs-fuse-csi
          mountPath: /mnt/gcs
      volumes:
      - name: config-volume
        configMap:
          name: axolotl-config
      - name: credentials
        secret:
          secretName: gcp-credentials
      - name: model-storage
        persistentVolumeClaim:
          claimName: model-storage
      - name: gcs-fuse-csi
        csi:
          driver: gcsfuse.csi.storage.gke.io
          volumeAttributes:
            bucketName: your-project-melanoma-dataset
            mountOptions: "implicit-dirs"